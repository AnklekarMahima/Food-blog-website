<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/styles.css">
    <title>Create New Food Post</title>
</head>
<body>
    <header>
        <h1>Create a New Food Blog Post</h1>
        <nav>
            <a href="/">Home</a>
            <a href="/create">Create New Post</a>
        </nav>
    </header>

    <main>
        <form action="/create" method="POST" enctype="multipart/form-data">
            <label for="title">Title:</label>
            <input type="text" id="title" name="title" placeholder="Delicious Recipe" required>

            <label for="description">Short Description:</label>
            <textarea id="description" name="description" placeholder="Describe your food post" maxlength="150" required></textarea>
            <p id="char-count" style="margin-top: 0;">0/150 characters</p>

            <label for="content">Content:</label>
            <textarea id="content" name="content" placeholder="Share the recipe, cooking methods, and tips!" required></textarea>

            <label for="image">Upload Image:</label>
            <input type="file" id="image" name="image" accept="image/*" required>

            <button type="submit">Create Post</button>
        </form>
        <script>
            const textarea = document.getElementById('description');
            const charCountDisplay = document.getElementById('char-count');
            const maxLength = 150;
    
            textarea.addEventListener('input', function() {
                const currentLength = textarea.value.length;
                charCountDisplay.textContent = `${currentLength}/${maxLength} characters`;
            });
        </script>
    </main>

    <footer>
        <p>&copy; 2024 Foodie's Haven | All rights reserved.</p>
    </footer>
</body>
</html>






